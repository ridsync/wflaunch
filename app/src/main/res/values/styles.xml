<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/main_primary_color</item>
        <item name="colorPrimaryDark">@color/main_primary_dark_color</item>
        <item name="colorAccent">@color/main_accent_color</item>
        <!-- Fonts -->
        <item name="android:textViewStyle">@style/TextViewStyle</item>
        <item name="android:buttonStyle">@style/ButtonStyle</item>
        <item name="android:editTextStyle">@style/EditTextStyle</item>
        <item name="android:radioButtonStyle">@style/RadioButtonStyle</item>
        <item name="android:checkboxStyle">@style/CheckboxStyle</item>

        <item name="android:windowContentTransitions">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Dialog (all version) -->
    <style name="TransparentDialog" parent="AppTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowCloseOnTouchOutside">false</item>
    </style>

    <style name="MainCustomDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Used for the Title and text -->
        <item name="android:textColorPrimary">@color/text_color_1f</item>
        <!-- Used for the buttons -->
        <item name="colorAccent">@color/main_accent_color</item>
        <!-- Used for the background -->
        <item name="android:windowBackground">@color/main_transparent</item>
        <item name="android:background">@color/main_transparent</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:windowFullscreen">true</item>
        <!-- Fonts -->
        <item name="android:textViewStyle">@style/TextViewStyle</item>
        <item name="android:buttonStyle">@style/ButtonStyle</item>
        <item name="android:editTextStyle">@style/EditTextStyle</item>
        <item name="android:radioButtonStyle">@style/RadioButtonStyle</item>
        <item name="android:checkboxStyle">@style/CheckboxStyle</item>
    </style>

    <style name="FullScreenDialog" parent="AppTheme">
        <!-- Used for the Title and text -->
        <item name="android:textColorPrimary">@color/text_color_1f</item>
        <!-- Used for the buttons -->
        <item name="colorAccent">@color/main_accent_color</item>
        <!-- Used for the background -->
        <item name="android:windowBackground">@color/main_dialog_dim_color</item>
        <item name="android:background">@color/main_transparent</item>
        <item name="android:windowFullscreen">false</item>
    </style>

    <!-- EditText -->
    <style name="StyledTilEditTextFloatingLabel" parent="AppTheme">
        <item name="colorControlNormal">@color/colorControlNormal</item>
        <item name="colorControlActivated">@color/colorControlNormal</item>
        <item name="colorControlHighlight">@color/colorControlHighlight</item>
    </style>

    <style name="CustomEditText" parent="AppTheme">
        <item name="colorControlNormal">@color/main_appbar_boxline</item>
        <item name="colorControlActivated">@color/main_accent_color</item>
        <item name="colorControlHighlight">@color/main_accent_color</item>
    </style>

    <style name="TutorialTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/main_transparent</item>
        <item name="android:windowTranslucentStatus">true</item>

    </style>

    <!-- Change Tab text appearance -->
    <style name="CustomTabWidgetText"
        parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="android:textSize">21dp</item>
    </style>

    <!--  (Ripple Effect) -->
    <style name="RippleEffect">
        <item name="android:foreground">?attr/selectableItemBackground</item>
    </style>

    <!-- 미사용 -->
    <style name="RippleEffect.WhiteBg" parent="Widget.AppCompat.Button.Borderless.Colored">
        <item name="android:background">@drawable/raised_button_bg_white</item>
    </style>

    <style name="RippleEffect.MainBg" parent="Widget.AppCompat.Button.Borderless.Colored">
        <item name="android:background">@drawable/raised_button_bg_main_bg</item>
    </style>

    <style name="RippleEffect.AccentBg" parent="Widget.AppCompat.Button.Borderless.Colored">
        <item name="android:textColor">@color/text_color_white</item>
        <item name="android:background">@drawable/raised_button_accent_color</item>
    </style>

    <style name="RippleEffect.GrayBg" parent="Widget.AppCompat.Button.Borderless.Colored">
        <item name="android:textColor">@color/text_color_white</item>
        <item name="android:background">@drawable/raised_button_gray_color</item>
    </style>

    <!-- Custom font style 일괄적용 -->
    <style name="TextViewStyle" parent="@android:style/Widget.DeviceDefault.TextView">
        <item name="android:includeFontPadding">false</item>
    </style>

    <style name="ButtonStyle" parent="@android:style/Widget.DeviceDefault.Button.Borderless">
        <item name="android:includeFontPadding">false</item>
    </style>

    <style name="EditTextStyle" parent="@android:style/Widget.DeviceDefault.EditText">
        <item name="android:includeFontPadding">false</item>
    </style>

    <style name="RadioButtonStyle" parent="@android:style/Widget.DeviceDefault.CompoundButton.RadioButton">
        <item name="android:includeFontPadding">false</item>
    </style>

    <style name="CheckboxStyle" parent="@android:style/Widget.DeviceDefault.CompoundButton.CheckBox">
        <item name="android:includeFontPadding">false</item>
    </style>

</resources>
